<% provide(:title, "Edit quote: #{@quote.title}") %>
<% provide(:button_text, 'Edit') %>

<main>
  <hgroup class="pb-0">
    <h1>Quotes</h1>
    <h2>Edit quote: <%= @quote.title %></h2>
  </hgroup>
  <section class="max-w-xl my-3 mx-auto bg-white border border-gray-400 shadow rounded-lg py-4">
  <%= render 'form' %>
  </section>
  <%= link_to 'Show', @quote %> |
  <%= link_to 'Back', quotes_path %>
</main>