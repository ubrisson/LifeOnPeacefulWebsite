<% provide(:title, @resource.title) %>

<main
  class="mt-12 lg:mt-6 pb-8 w-full max-w-2xl mx-auto w-full
  px-4 bg-transparent text-center shadow-none border-0">
  <%= render @resource %>

  <%= link_to 'Back', resources_path %>
</main>