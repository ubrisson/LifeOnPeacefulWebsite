<% if admin? %>
  <details class="my-4 rounded bg-primary border border-primary hover:border-secondary">
    <summary class="px-3 py-3 text-sm font-bold rounded-t text-left
           text-default-soft hover:bg-secondary">
      New <%= controller_name.singularize %>
    </summary>
    <%= render 'form' %>
  </details>
<% end %>