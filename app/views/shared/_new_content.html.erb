<% if admin? %>
  <details class="my-4 bg-white border border-gray-400 rounded hover:border-blue-400">
    <summary class="px-3 py-3 text-sm font-bold text-gray-600 rounded-t text-left
          hover:bg-indigo-100 hover:text-blue-900">
      New <%= controller_name.singularize %>
    </summary>
    <%= render 'form' %>
  </details>
<% end %>